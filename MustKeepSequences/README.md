Reference sequences (fasta) that must survive non-redundancy filtering
regardless of similarity to other sequences, e.g. a well-annotated
reference sequence that a standalone check or `_SpecialAminoAcids.txt`
numbers an amino acid position of interest against.

Delete this file once real sequences have been added; it only exists to
keep this otherwise-empty directory tracked in git.
