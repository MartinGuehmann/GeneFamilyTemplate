Put the bait/seed sequences for this gene family here, one FASTA file per
sequence, e.g. `AAA32805_Ga1_Arabidopsis_thaliana.fasta`. These are the
sequences used to search the protein databases for hits (see
`00a_GetGenes.sh` in PhylogenyPipeline).

Delete this file once real bait sequences have been added; it only exists
to keep this otherwise-empty directory tracked in git.
